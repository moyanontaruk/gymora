# AI-generated file (Claude, Anthropic) — pytest configuration for the Gymora backend test suite.
# Adds the backend/ folder to sys.path so tests can `import app...` the same way the app does.
[pytest]
pythonpath = .
testpaths = tests
